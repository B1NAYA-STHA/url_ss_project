# URL Screenshot App

A simple React frontend application built with **Vite** that allows users to input a URL and view a screenshot of that webpage. The app communicates with Express backend that handles screenshot generation.

---
